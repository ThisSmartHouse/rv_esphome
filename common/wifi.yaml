wifi:
  ap:
    ssid: "${device_name}"
    password: !secret wifi_password_1
  reboot_timeout: 120s
  power_save_mode: none
  fast_connect: true
  networks:
    - ssid: !secret wifi_ssid_1
      password: !secret wifi_password_1

